<div class="position container-fluid">
    <div class="d-flex justify-content-center pt-4">
      <div class="profile-user-infos col-8 mb-2">
        <h2>Salut <%= @profile.username %>,</h2>
          <p>Mon score: <strong><%= @profile.score %></strong> pts <i class="fas fa-star"></i></p>
          <p><strong><%= @profile.participations.currents.count%></strong> quêtes en cours <i class="fas fa-check"></i>
          <p>Mon empreinte carbone: <strong><%= ((@profile.foot_print) / 100).round(1) %></strong> T / C02 e <i class="fas fa-bolt"></i></h2>
      </div>
    </div>
    <%= render "./participations/participations_index_partial", locals: {participations_past: @my_past_participations, participations_current: @my_current_participations} %>
  </div>
</div>
